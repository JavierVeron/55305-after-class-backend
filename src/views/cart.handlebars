<div class="container">
    <div class="row">
        <div class="col-md-12">
            <table class="table">
                {{#each products.payload}}
                    <tr>    
                        <td>
                            <a href="/products/{{this._id}}" class="text-decoration-none">
                                <img src="{{this.thumbnails}}" class="img-fluid" alt="{{this.title}}">
                            </a>
                        </td>
                        <td>{{this.title}}</td>
                        <td>${{this.price}}</td>
                        <td><button class="btn btn-dark" onclick="eliminarProducto('{{product._id}}')">Eliminar (-)</button></td>
                    </tr>
                {{/each}}
            </table>
        </div>
    </div>
</div>